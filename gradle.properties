# Heap for the Gradle daemon (Loom remapJar / TinyRemapper benefits from 2G+).
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
# ---------------------------------------------------------------------------
# Fabric Properties
# ---------------------------------------------------------------------------
# As of Minecraft 26.1 the game ships DEOBFUSCATED with Mojang's official names.
# Yarn mappings are no longer provided by Fabric, so the `yarn_mappings` line
# has been removed entirely. The build uses the new non-remapping Loom plugin
# (`net.fabricmc.fabric-loom` 1.17). See PATCH_NOTES.md for the full migration.
#
# Recommended versions are published on https://fabricmc.net/develop/
minecraft_version=26.2
loader_version=0.19.3
# Loom 1.17 is published as a SNAPSHOT (no release build yet); see
# https://maven.fabricmc.net/net/fabricmc/fabric-loom/net.fabricmc.fabric-loom.gradle.plugin/
loom_version=1.17-SNAPSHOT
# Mod Properties
mod_version=2.3.0
maven_group=com.github.xandergos
archives_base_name=terrain-diffusion-mc
# Dependencies
fabric_version=0.152.2+26.2
